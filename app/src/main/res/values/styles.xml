<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="Theme.SpotifyWrapped" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:state_focused">@color/button_light</item>
        <item name="android:colorFocusedHighlight">@color/button_light</item>
    </style>

</resources>